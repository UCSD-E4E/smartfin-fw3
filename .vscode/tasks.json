{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "clean",
            "command": "cmake --build ${workspaceFolder}/build --target clean",
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "problemMatcher": []
        }
        
        
        
    ]
}